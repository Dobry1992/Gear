@page
@model Gear.Pages.Admin.DeleteRoleModel
@{
    ViewData["Title"] = "Delete Role";
}

<h1>Удаление</h1>
<h3>Вы уверены, что желаете удалить данную роль <span class="text-danger">@Html.DisplayFor(model => model.Role.Name)</span>?</h3>
<form method="post">
    <input type="hidden" asp-for="Role.Id" />
    <input type="submit" value="Удалить" class="btn btn-danger" />
    <a class="btn btn-primary" asp-page="./Index">Назад</a>
</form>
